<% if (typeof activeScripts !== 'undefined' && activeScripts.footer && activeScripts.footer.length) { %>
<% activeScripts.footer.forEach(function (scriptContent) { %>
<%- scriptContent %>
<% }); %>
<% } %>
